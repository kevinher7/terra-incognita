# Python
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
*.egg-info/
build/
dist/

# uv / venv
.venv/

# env / secrets
.env
.env.*
!.env.example

# project data & artifacts (never commit datasets/weights — they live in S3)
/data/
/artifacts/
/mlruns/
/mlartifacts/
# Ultralytics train outputs + auto-downloaded pretrained weights (a training-smoke run drops
# these in the CWD; the real weights are logged to MLflow/S3, never committed).
/runs/
*.pt

# local stack: pinned official SigNoz, fetched on `just up` (not vendored — see justfile)
/.signoz/

# OS / editor
.DS_Store
